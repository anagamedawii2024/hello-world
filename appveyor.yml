version: 1.0.{build}

image: Ubuntu

clone_depth: 1

environment:
  nodejs_version: 16

install:
  # Install Node.js
  - sudo apt-get update
  - curl -sL https://deb.nodesource.com/setup_16.x | sudo -E bash -
  - sudo apt-get install -y nodejs

